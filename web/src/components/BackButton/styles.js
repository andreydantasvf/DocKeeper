import styled from "styled-components";

export const Container = styled.div`
  > a {
    width: fit-content;
    border-radius: .8rem;
    margin-top: 2rem;
    color: #FFF;
    background: #464242;
    padding: 1.3rem 2rem;
    font-size: 1.6rem;
    display: flex;
    align-items: center;
    gap: .8rem;
  }
  > svg {
    font-size: 2.4rem;
    color: #FFF;
  }
`;
